import XCTest
@testable import protoor_bloackchain

final class protoor_bloackchainTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(protoor_bloackchain().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
